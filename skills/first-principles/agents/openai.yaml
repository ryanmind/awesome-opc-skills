interface:
  display_name: "First Principles"
  short_description: "Analyze hard decisions from first principles"
  default_prompt: "Use $first-principles to separate real constraints from inherited assumptions, rebuild this decision from zero, and define the next test or action."

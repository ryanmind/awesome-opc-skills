interface:
  display_name: "Git Commit"
  short_description: "Draft repo-aware Git commit messages"
  default_prompt: "Use $git-commit when I say git commit, commit these changes, or ask for a repo-aware commit message."
